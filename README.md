# Implementations of some data structs and algos

- `bst.c` - binary search tree

- `rbtree.c` - red-black tree

- `hashtable.c` - hash with chaining using fnv hash function

To build everything:

```sh
make
```
